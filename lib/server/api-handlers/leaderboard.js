import {sqlClient,json} from '../db.js';
export default async function handler(req,res){try{if(req.method!=='GET')return json(res,405,{ok:false});const sql=sqlClient();const rows=await sql`SELECT u.nick,u.name,u.xp,u.streak FROM users u LEFT JOIN privacy_settings p ON p.user_id=u.id WHERE u.status='active' AND COALESCE(p.show_leaderboard,true)=true ORDER BY u.xp DESC,u.updated_at ASC LIMIT 100`;return json(res,200,{ok:true,rows})}catch(e){return json(res,500,{ok:false,error:'Leaderboard failed'})}}
