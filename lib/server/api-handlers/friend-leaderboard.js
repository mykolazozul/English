import {sqlClient,json,requireUser} from '../db.js';
export default async function handler(req,res){try{const me=await requireUser(req,res);if(!me)return json(res,401,{ok:false,error:'Unauthorized'});const sql=sqlClient();const rows=await sql`SELECT u.nick,u.name,u.xp,u.streak FROM users u LEFT JOIN privacy_settings p ON p.user_id=u.id WHERE (u.id=${me.id} OR u.id IN (SELECT CASE WHEN f.user_id=${me.id} THEN f.friend_id ELSE f.user_id END FROM friendships f WHERE (f.user_id=${me.id} OR f.friend_id=${me.id}) AND f.status='accepted')) AND u.status='active' AND (u.id=${me.id} OR COALESCE(p.show_leaderboard,true)=true) ORDER BY u.xp DESC,u.updated_at ASC LIMIT 100`;return json(res,200,{ok:true,rows})}catch(e){return json(res,500,{ok:false,error:'Friends leaderboard failed'})}}
