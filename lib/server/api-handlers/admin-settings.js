import {sqlClient,json,parseBody,requireAdmin} from '../db.js';
export default async function handler(req,res){try{const admin=await requireAdmin(req,res,'settings.manage');if(!admin)return json(res,401,{ok:false,error:'Unauthorized'});const sql=sqlClient();if(req.method==='GET'){const rows=await sql`SELECT key,value FROM admin_settings ORDER BY key`;return json(res,200,{ok:true,settings:Object.fromEntries(rows.map(r=>[r.key,r.value]))})}if(req.method==='PUT'){const settings=parseBody(req).settings||{};const allowed=['lessonSize','correctPoints','wrongPoints','masteryThreshold','shuffleQuestions','shuffleAnswers','showPronunciation','maintenanceMode','maxLessonSize','srsVersion','perfectBonus','badgeStyle'];for(const [key,value] of Object.entries(settings)){if(!allowed.includes(key))continue;await sql`INSERT INTO admin_settings(key,value,updated_at) VALUES(${key},${JSON.stringify(value)}::jsonb,now()) ON CONFLICT(key) DO UPDATE SET value=excluded.value,updated_at=now()`}await sql`INSERT INTO admin_audit_logs(admin_user_id,action,metadata) VALUES(${admin.id},'settings.update',${JSON.stringify({keys:Object.keys(settings).filter(k=>allowed.includes(k))})}::jsonb)`;return json(res,200,{ok:true})}return json(res,405,{ok:false})}catch(e){return json(res,500,{ok:false,error:'Admin settings failed'})}}
