import {sqlClient,json,requireAdmin} from '../lib/server/db.js';
export default async function handler(req,res){try{const admin=await requireAdmin(req,res);if(!admin)return json(res,403,{ok:false});const sql=sqlClient();const t0=Date.now();await sql`SELECT 1`;const dbMs=Date.now()-t0;const [sessions,queue,security,reports]=await Promise.all([sql`SELECT count(*)::int AS n FROM sessions WHERE expires_at>now()`,sql`SELECT count(*)::int AS n FROM progress_events WHERE created_at>now()-interval '1 hour'`,sql`SELECT count(*)::int AS n FROM security_events WHERE created_at>now()-interval '24 hours'`,sql`SELECT count(*)::int AS n FROM reports WHERE status='open'`]);return json(res,200,{ok:true,dbMs,activeSessions:sessions[0]?.n||0,progressLastHour:queue[0]?.n||0,security24h:security[0]?.n||0,openReports:reports[0]?.n||0,at:new Date().toISOString()})}catch(e){return json(res,500,{ok:false,error:'Monitoring failed'})}}
